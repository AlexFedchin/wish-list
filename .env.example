# Signing key for session tokens. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
# Set it in Netlify under Site configuration -> Environment variables.
JWT_SECRET=
